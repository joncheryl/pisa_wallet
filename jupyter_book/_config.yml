# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: Education Dataset vs Civic Honesty
author: John Sherrill
logo: logo.png

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: force

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: book.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
  - references.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/joncheryl/pisa_wallet/  # Online location of your book
  path_to_book: jupyter-book  # Optional path to your book, relative to the repository root
  branch: master  # Which branch of the repository should be used when creating links (optional)

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
html:
  use_issues_button: true
  use_repository_button: true

# From https://jupyterbook.org/en/stable/interactive/interactive.html
# For these plots to show, it may be necessary to load require.js, in your
# _config.yml: 
# 
# and
# 
# Since jupyter-book version 0.14, showing plotly figures will generate a warning 
# when building the book. You can suppress this warning by adding the following to 
# your _config.yml:
# sphinx:
#   config:
#     html_js_files:
#     - https://cdnjs.cloudflare.com/ajax/libs/require.js/2.3.7/require.min.js
    # suppress_warnings: ["mystnb.unknown_mime_type"]
